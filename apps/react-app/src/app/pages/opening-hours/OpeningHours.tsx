import { injectIntl, WrappedComponentProps } from "react-intl";

export const OpeningHours = (props: WrappedComponentProps) => {
  return <>Opening hours</>;
};

export default injectIntl(OpeningHours);
